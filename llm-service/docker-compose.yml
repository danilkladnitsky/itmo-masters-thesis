version: "3.8"

services:
  ollama:
    image: ollama/ollama
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped
    entrypoint: ["/bin/sh", "-c", "ollama serve & sleep 3 && ollama run qwen3:0.6b --hidethinking && tail -f /dev/null"]
    deploy:
      resources:
        limits:
          cpus: '1.75'       # Leaves headroom for the OS
          memory: 4g         # Keeps ~2GB free for system & Docker overhead
        reservations:
          cpus: '1.25'
          memory: 2g

volumes:
  ollama_data: